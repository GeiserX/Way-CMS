[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["cms"]
omit = [
    "cms/venv/*",
    "cms/test_multitenancy.py",
]

[tool.coverage.report]
exclude_lines = [
    "if __name__",
    "pragma: no cover",
]
